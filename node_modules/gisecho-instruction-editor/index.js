/*
 Instructions editor makes instruction for various applications e.g  Trip, LBC (Location Based Calendar),
 RPLC(real time location chat)
*/

var express = require('express');
var fs = require("fs");
var path = require('path');

var editorRouter = express.Router();
var appDir = path.dirname(require.main.filename);

editorRouter.get('/', function(req, res) {
  res.sendFile(appDir + '/public/view/test.html');
});


module.exports = editorRouter;